# ========================================
# Docker Compose Environment Configuration
# ========================================
# This file contains environment variables for docker-compose services
# Copy this to .env in the project root before running docker-compose

# ========================================
# PostgreSQL (Main Database)
# ========================================
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=bn88

# ========================================
# LINE Engagement Platform Database
# ========================================
LEP_POSTGRES_USER=lep
LEP_POSTGRES_PASSWORD=lep_password
LEP_POSTGRES_DB=line_platform

# ========================================
# Application URLs (for inter-service communication)
# ========================================
BACKEND_URL=http://backend:3000
FRONTEND_URL=http://frontend:5555
LEP_URL=http://lep:8080
