# Copy this file to .env before running Vite
# The Vite dev server uses these values at build/runtime

# API base URLs - points to the backend API
# When using Vite proxy (recommended for development), use /api
# The proxy in vite.config.ts will forward /api -> http://localhost:3000
VITE_API_BASE=http://127.0.0.1:3000/api
VITE_ADMIN_API_BASE=http://127.0.0.1:3000/api

# Optional alias for some setups/tools
VITE_API_BASE_URL=http://127.0.0.1:3000

# Tenant configuration - must match backend tenant
VITE_TENANT=bn9
VITE_DEFAULT_TENANT=bn9
VITE_DEFAULT_BOT_ID=dev-bot

# Build metadata
VITE_APP_VERSION=dev

# Optional admin helper (used occasionally for maintenance only)
# Leave empty for normal operation
VITE_ADMIN_CODE=

# Optional OpenAI keys for local AI-assisted workflows
# Leave empty to disable AI features in the frontend
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini
